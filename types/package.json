{
  "name": "types",
  "version": "1.0.0",
  "main": "dist/src/index.d.ts",
  "types": "dist/src/index.d.ts",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p tsconfig.json"
  }
}
